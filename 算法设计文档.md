## 4.4 算法设计

本系统使用到的算法主要有三个，即五维加权综合评价算法、K-Means++ 聚类分析算法和聚类质量评估算法。下面对这三个算法的实现进行简单介绍。

### 4.4.1 五维加权综合评价算法

本系统基于德育、智育、体育、美育、劳动教育五个维度对学生进行综合评价。每个维度独立计算总分后，通过加权汇总的方式得到综合评分。该算法的核心思想是：不同维度在综合素质评价中的重要程度不同，因此赋予不同的权重系数。综合评分计算如公式 4-1 所示。

$$
\begin{equation}
S_{total} = W_1 \times S_{moral} + W_2 \times S_{academic} + W_3 \times S_{physical} + W_4 \times S_{art} + W_5 \times S_{practice}
\tag{4-1}
\end{equation}
$$

式中：$S_{total}$ 表示学生综合评分，$S_{moral}$、$S_{academic}$、$S_{physical}$、$S_{art}$、$S_{practice}$ 分别表示德育、智育、体育、美育、劳动教育五个维度的评分，$W_1$ 至 $W_5$ 为对应维度的权重系数。

系统中各维度权重配置为：德育 $W_1 = 0.25$，智育 $W_2 = 0.50$，体育 $W_3 = 0.08$，美育 $W_4 = 0.08$，劳动教育 $W_5 = 0.09$，权重之和为 $1.00$。其中智育权重最高，体现了学业成绩在综合素质评价中的核心地位。

各维度评分由子项加总计算得出。以德育为例，其计算公式如公式 4-2 所示。

$$
\begin{equation}
S_{moral} = \frac{S_{base} + S_{moral\_{sub}} + S_{bonus}}{3} - S_{deduct}
\tag{4-2}
\end{equation}
$$

式中：$S_{base}$ 为实践基础分，$S_{moral\_{sub}}$ 为品德表现小计（含政治思想、诚实守信、学习态度、纪律遵守、集体意识、文明礼貌六项），$S_{bonus}$ 为奖励分小计（含德育荣誉、社会工作、突出表现三项），$S_{deduct}$ 为扣分项。其余四个维度采用类似结构进行计算，所有评分均确保最小值为 $0$。

### 4.4.2 K-Means++ 聚类分析算法

利用 K-Means++ 聚类算法，系统可以自动将学生按综合素质特征进行分组，识别出不同发展类型的学生群体。该算法在标准 K-Means 基础上改进了初始质心的选取策略，使聚类结果更加稳定可靠。算法的完整流水线包含数据预处理、降维、聚类和结果命名四个阶段。

#### （1）Z-Score 标准化

由于五个维度的评分量纲和取值范围不同，首先对原始特征矩阵进行 Z-Score 标准化处理，如公式 4-3 所示。

$$
\begin{equation}
z_{ij} = \frac{x_{ij} - \bar{x}_j}{s_j}
\tag{4-3}
\end{equation}
$$

式中：$x_{ij}$ 表示第 $i$ 个学生在第 $j$ 个维度上的原始评分，$\bar{x}_j$ 表示第 $j$ 个维度的均值，$s_j$ 表示第 $j$ 个维度的样本标准差（使用 $n-1$ 作为分母）。标准化后的数据被裁剪至 $[-3.0, 3.0]$ 范围内，以消除极端值的影响。

#### （2）PCA 降维

标准化后的数据通过主成分分析（PCA）进行降维。系统采用奇异值分解（SVD）方法，计算各主成分的方差贡献率，选取累积方差占比达到 $90\%$ 的主成分数量，且最少保留 $2$ 个维度。降维过程如公式 4-4 所示。

$$
\begin{equation}
Z_{reduced} = Z_{normalized} \times V_{[:, 1:k]}
\tag{4-4}
\end{equation}
$$

式中：$V$ 为 SVD 分解得到的右奇异向量矩阵，$k$ 为满足累积方差阈值的主成分数。降维旨在去除特征间的冗余信息，提升聚类效率。

#### （3）K-Means++ 聚类

降维后的数据使用 Apache Commons Math3 库的 KMeansPlusPlusClusterer 进行聚类，最大迭代次数为 $300$，距离度量采用欧氏距离，随机种子固定为 $42$ 以保证结果可复现。K-Means++ 的核心改进在于初始质心的选取：第一个质心随机选择，后续质心以与已有质心距离的平方成正比的概率选取，使得初始质心尽可能分散。

最优 K 值通过四种指标投票确定：轮廓系数（Silhouette Score）取最大值对应的 K，戴维森堡丁指数（Davies-Bouldin Index）取最小值对应的 K，卡林斯基-哈拉巴斯指数（Calinski-Harabasz Index）取最大值对应的 K，Gap Statistic 独立计算最优 K。最终以投票出现次数最多的 K 值作为聚类数。

Gap Statistic 的计算公式如公式 4-5 所示。

$$
\begin{equation}
Gap(K) = \frac{1}{B}\sum_{b=1}^{B}\log(W_{kb}^*) - \log(W_k)
\tag{4-5}
\end{equation}
$$

式中：$W_k$ 为原始数据在 $K$ 个簇下的簇内离差和，$W_{kb}^*$ 为第 $b$ 个 Monte Carlo 参考数据集的簇内离差和，$B = 10$ 为参考数据集数量。参考数据集在原始数据各维度的最小值与最大值范围内均匀随机生成。选择满足 $Gap(K) \geq Gap(K+1) - s_{k+1}$ 的最小 $K$ 值作为最优聚类数。

#### （4）聚类结果命名

聚类完成后，系统通过 ClusterNamingService 根据各簇在五个维度上的均值排名和绝对分数对簇进行语义化命名。命名策略分为三大类：高排名簇根据优势维度组合命名为"全面优秀型""品学兼优型""学术艺术型"等；低排名簇命名为"待提升型""基础提升型"等；中间簇根据特征命名为"均衡发展型""学术偏强型"等。命名阈值采用动态计算，根据所有簇均值排序后的三分位点确定。

### 4.4.3 聚类质量评估算法

为确保聚类结果的科学性和可靠性，系统采用三种指标对聚类质量进行综合评估。

#### （1）轮廓系数（Silhouette Score）

轮廓系数衡量样本与其自身簇的紧密程度和与其他簇的分离程度，如公式 4-6 所示。

$$
\begin{equation}
s(i) = \frac{b(i) - a(i)}{\max(a(i),\, b(i))}
\tag{4-6}
\end{equation}
$$

式中：$a(i)$ 为样本 $i$ 与同簇其他样本的平均距离，$b(i)$ 为样本 $i$ 与其他所有簇的平均距离中的最小值。$s(i)$ 取值范围为 $[-1, 1]$，越接近 $1$ 表示聚类效果越好。最终得分为所有样本轮廓系数的均值。

#### （2）戴维森堡丁指数（Davies-Bouldin Index）

DBI 衡量簇间分离度与簇内紧密度的比值，如公式 4-7 所示。

$$
\begin{equation}
DB = \frac{1}{K}\sum_{i=1}^{K}\max_{j \neq i}\frac{\sigma_i + \sigma_j}{d(c_i,\, c_j)}
\tag{4-7}
\end{equation}
$$

式中：$\sigma_i$ 为簇 $i$ 内样本到质心的平均距离，$d(c_i, c_j)$ 为簇 $i$ 与簇 $j$ 质心间的欧氏距离。DBI 值越小表示聚类效果越好。

#### （3）卡林斯基-哈拉巴斯指数（Calinski-Harabasz Index）

CHI 通过组间离差与组内离差的比值评估聚类质量，如公式 4-8 所示。

$$
\begin{equation}
CH = \frac{SS_b / (K-1)}{SS_w / (n-K)}
\tag{4-8}
\end{equation}
$$

式中：$SS_b$ 为组间离差平方和，$SS_w$ 为组内离差平方和，$K$ 为聚类数，$n$ 为样本总数。CHI 值越大表示聚类效果越好。

此外，系统还对聚类结果执行单因素方差分析（ANOVA），检验各维度在不同簇间是否存在显著差异。ANOVA 的 F 统计量如公式 4-9 所示。

$$
\begin{equation}
F = \frac{MS_b}{MS_w} = \frac{SS_b / (K-1)}{SS_w / (n-K)}
\tag{4-9}
\end{equation}
$$

$p$ 值通过正则化不完全 Beta 函数近似计算，效应量采用 $\eta^2 = SS_b / (SS_b + SS_w)$ 衡量。当 $p$ 值小于显著性水平时，认为该维度在不同簇间存在显著差异，聚类结果具有统计意义。
